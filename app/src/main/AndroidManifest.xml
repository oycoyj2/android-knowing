<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/knowing_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/knowing_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Knowingsimple"
        tools:targetApi="31">
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.main.CategorySelectionActivity" />
        <activity android:name=".ui.quiz.SingleCategoryQuizActivity" />
        <activity android:name=".ui.quiz.CategorySelectionQuizActivity" />
        <activity android:name=".ui.quiz.QuizResultActivity" />
        <activity android:name=".ui.addquiz.AddQuizActivity" />
        <activity android:name=".ui.addquiz.AddQuizActivityWithoutCategory" />
        <activity android:name=".ui.quiz.QuizListActivity" />
        <activity android:name=".ui.quiz.QuizDetailActivity" />
        <activity android:name=".ui.quiz.QuizEditActivity" />


    </application>

</manifest>